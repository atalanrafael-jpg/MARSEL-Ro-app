# MARSEL ROAPP — TASK REGISTER

Only active tasks belong here. Historical tasks belong in `06_ARCHIVE` or Git history.

## BLOCKED / NEEDS VERIFICATION
- Obtain required review for canonical cleanup PR.
- Run protected `main` CI after merge.
- Verify staging-scoped `ROAPP_API_KEY` through the canonical UCP.
- Produce real backup evidence from the verified read-only UCP chain.
- Verify restore/integrity evidence before any production-write consideration.

## RULE
No task is marked complete without current direct evidence.
